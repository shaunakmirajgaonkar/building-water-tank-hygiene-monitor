# Data Dictionary
Required tank fields are defined in `watertank_engine.py` and represented in `data/sample_tank_metrics.csv`. History fields are represented in `data/sample_hygiene_history.csv` and may repeat a `tank_id` across dates.
